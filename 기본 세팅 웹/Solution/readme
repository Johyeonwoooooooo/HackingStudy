취약점 2가지를 해결해보았다.

# 1. ID, PW 검증 시 or 사용
이 문제는 매우 간단하게 해결 가능하다. 
if문의 조건 중 id, pw가 모두 같을 때 이동할 수 있도록 and로 변경한다.

# 2. url에 바로 입력하기
이 문제는 쿠키로 해결하였다.
if문 조건에서 id,pw가 일치하면, 해당 사용자에게 쿠키를 저장하게 하고, login-success.php파일에서 쿠키가 없으면 다른 페이지로 이동하게 하였다.

